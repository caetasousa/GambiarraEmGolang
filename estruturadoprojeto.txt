


💻 Estruturas (Structs) do Domínio em Go
1. Entidades Principais
📅 Agendamento
Representa a reserva do serviço.

Go

type Agendamento struct {
    ID                string
    ClienteID         string
    PrestadorID       string
    ServicoID         string
    DataHoraInicio    time.Time
    DataHoraFim       time.Time // Calculado com base na duração do Serviço
    Status            string    // Ex: "Confirmado", "Cancelado", "Pendente"
    Notas             string
}
🧑‍💼 PrestadorDeServico
O recurso cuja agenda é gerenciada.

Go

type PrestadorDeServico struct {
    ID                string
    Nome              string
    Email             string
    Telefone          string
    // Disponibilidade é um Value Object que pode ser carregado separadamente
}
👤 Cliente
A pessoa que realiza o agendamento.

Go

type Cliente struct {
    ID                string
    Nome              string
    Email             string
    Telefone          string
}
🛠️ Servico
O tipo de serviço oferecido.

Go

type Servico struct {
    ID                string
    Nome              string
    Descricao         string
    DuracaoEmMinutos  int // Essencial para calcular DataHoraFim
    PrecoCents        int // Usar inteiro (centavos) para evitar problemas de ponto flutuante
}
2. Objetos de Valor
Estes são conceitos que não têm ID próprio, mas são cruciais para descrever as entidades e regras.

⏱️ IntervaloDeTempo
Usado para representar um período de tempo, seja um slot livre ou um agendamento.

Go

type IntervaloDeTempo struct {
    Inicio    time.Time
    Fim       time.Time
}
🗓️ DisponibilidadeSemanal
Representa a regra de funcionamento regular de um Prestador.

Go

type DisponibilidadeSemanal struct {
    PrestadorID  string
    DiaDaSemana  string // Ex: "SEGUNDA", "TERCA"
    Intervalos   []IntervaloDiario // Lista de horários de trabalho naquele dia
}

type IntervaloDiario struct {
    HoraInicio string // Ex: "09:00"
    HoraFim    string // Ex: "12:00"
}